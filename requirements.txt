# ── LangGraph & LangChain stack ──────────────────────────────────────────────
# Core dependencies pinned for Python 3.13 Windows with numpy<2.0 workaround:
#   numpy 2.1.0+ has no pre-built wheel for numpy<2.0 on Python 3.13+
#   Workaround: install numpy separately with --only-binary=:all:
#   then install langchain stack with --no-deps if needed
#
# Installation order (Windows Python 3.13):
#   pip install numpy==2.1.0 --only-binary=:all:
#   pip install -r requirements.txt

langgraph==0.1.19
langchain==0.2.17
langchain-core==0.2.43
langchain-openai==0.1.20

# LLM & networking
groq>=0.4.1
httpx==0.27.2

# Data validation & utilities
pydantic>=2.7.4
tenacity>=8.1.0
jsonpatch>=1.33
langsmith>=0.1.112
PyYAML>=6.0
packaging>=23.2
typing-extensions>=4.7
python-dotenv>=1.0.0
SQLAlchemy>=1.4
aiohttp>=3.8.3
dataclasses-json>=0.5.7
requests>=2.28

# PDF processing
pdfplumber>=0.10.0
pdf2image>=1.17.0
pytesseract>=0.3.10
Pillow>=10.0.0

# Graph processing
networkx>=3.0

# Web server
flask>=3.0.0

# Report generation
jinja2>=3.1.0
weasyprint>=60.0

# Vector store (optional — excluded from requirements due to C++ build tools requirement on Windows)
# chromadb>=0.4.0
# The system gracefully runs in rules-only mode without ChromaDB

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
